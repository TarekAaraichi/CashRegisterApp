public class Main {

    // The main method is the entry point of the program
    public static void main(String[] args) {

        // Initialize the CashRegister class, which handles the main POS system
        CashRegister cashRegister = new CashRegister();
        
        // Call the run method to start the CashRegister application
        cashRegister.run();
    }
}